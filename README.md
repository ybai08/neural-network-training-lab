# Neural Network Training Lab

Interactive MNIST neural-network teaching lab for students learning the basics of forward passes, loss, gradients, and backpropagation.

The regular neural-network lab is based on videos 1-4 of 3Blue1Brown's neural networks series and uses bundled MNIST IDX files served from `public/mnist/`.

